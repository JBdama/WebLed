String message = R"rawliteral(
<html><head><style>
#lv {
  background-color: #0000ff;
  filter: brightness(175%);
    width: 100%;
    height: 100%;
    position: absolute;
}
</style></head><body><i>Hello World</i>
  <p><button id="button" class="button">Toggle</button></p> <div id="lv" /> <script>
  var gateway = `ws://${window.location.hostname}/ws`;
  var websocket;
  window.addEventListener('load', onLoad);
  function rgbToHex(r, g, b) {
  return "#" + ((1 << 24) + (r << 16) + (g << 8) + b).toString(16).slice(1); }
  function initWebSocket() {
    console.log('Trying to open a WebSocket connection...');
    websocket = new WebSocket(gateway);
    websocket.onopen    = onOpen;
    websocket.onclose   = onClose;
    websocket.onmessage = onMessage; // <-- add this line
  }
  function onOpen(event) {
    console.log('Connection opened');
  }
  function onClose(event) {
    console.log('Connection closed');
    setTimeout(initWebSocket, 2000);
  }
  function onMessage(event) {
    var state;
    var mes = "";
    console.log(event.data);
    var rgb_arr = JSON.parse("["+event.data+"]");
    for (let x in rgb_arr) {mes+=rgbToHex(rgb_arr[x][0],rgb_arr[x][1],rgb_arr[x][2]); if(x<rgb_arr.length-1) mes+=",";}
    mes = "linear-gradient(90deg,"+mes+")";
    document.getElementById("lv").style.background = mes;
    
  }
  function onLoad(event) {
    initWebSocket();
    initButton();
  }
  function initButton() {
    document.getElementById('button').addEventListener('click', toggle);
  }
  function toggle(){
    console.log("ES toggelt");
    websocket.send('toggle');
    
  }

</script></body></html>
)rawliteral";
